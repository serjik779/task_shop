{% extends 'ShopBundle:Default:index.html.twig' %}
{% block body %}
    test
    {% if categories %}
        {% for category in categories %}
            {{ category.title }}
        {% endfor %}
    {% endif %}
{% endblock %}
